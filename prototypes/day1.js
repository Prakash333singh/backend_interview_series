// prototypes

// objects are created by constructors calls using new keyword
//classes are blueprints of objects
//objects are instances of classes

//in c++/java when we create a class and when we create an object from
// that class, and these objects are based on these classes

//but in js we don't have any mental model of copy
// objects are going to be linked to their classes

//what is prototype in js?
// it is mechanism using which js objects inherit fetaures from one another
//and every object having prototype property(and this prototype property is object itself and this prototype get attached to everyobject and even after if you make any changes to that prototype after the object has been created ,still it  get access to that prototype) is going to inherit those features
